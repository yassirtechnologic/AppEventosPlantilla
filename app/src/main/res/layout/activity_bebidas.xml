<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollBebidas"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/menuPanelOverlay">

        <!-- 🧃 Imagen de la categoría -->
        <ImageView
            android:id="@+id/ivCategoria"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:layout_marginBottom="16dp"
            android:src="@drawable/bebidas"
            android:contentDescription="@string/cd_bebidas" />

        <!-- 📝 Texto descriptivo -->
        <TextView
            android:id="@+id/tvDescripcionCategoria"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/bebidas_desc"
            android:textSize="18sp"
            android:lineSpacingExtra="4dp"
            android:textColor="@color/white"
            android:gravity="center"
            android:padding="12dp" />
    </LinearLayout>
</ScrollView>







<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/fondo">

    <!-- Solo visible para no-admin -->
    <TextView
        android:id="@+id/tvSoloAdmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/galeria_permiso_rationale"
        android:visibility="gone"
        android:paddingBottom="8dp" />

    <!-- Botón subir (lo ocultamos si no es admin desde el código) -->
    <Button
        android:id="@+id/btnSubirImagen"
        style="@style/Widget.App.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_subir_imagen" />

    <!-- Estado vacío -->
    <TextView
        android:id="@+id/tvVacio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/galeria_empty"
        android:visibility="gone"
        android:paddingTop="12dp" />

    <!-- Lista de imágenes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerGaleria"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:overScrollMode="ifContentScrolls"
        android:paddingTop="12dp"
        android:paddingBottom="8dp" />
</LinearLayout>









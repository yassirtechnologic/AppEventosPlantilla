<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AppEventos" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Colores base -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/colorOnPrimaryContainer</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorSecondaryContainer">@color/colorSecondaryContainer</item>
        <item name="colorOnSecondaryContainer">@color/colorOnSecondaryContainer</item>

        <!-- 👇 corregido: este debe ser android:colorBackground -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorOutline">@color/colorOutline</item>

        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- Tipografía -->
        <item name="android:fontFamily">sans-serif</item>

        <!-- Apariencias de texto -->
        <item name="textAppearanceHeadlineMedium">@style/TituloSeccion</item>
        <item name="textAppearanceBodyMedium">@style/TextoSecundario</item>

        <!-- Botón por defecto -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>

        <!-- Status/Navigation bar -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
    </style>

</resources>





